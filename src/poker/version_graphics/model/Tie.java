package poker.version_graphics.model;

import java.util.ArrayList;

public enum Tie {
    winner, looser;

   public static Tie finding(ArrayList<Player> players) {


       Tie temp = winner;
       return temp;
    }

    public static void highPair(ArrayList<Card> cards){
/*       boolean found = false;
        String found1;
        String found2;
        for (int i = 0; i < cards.size() - 1 && !found; i++) {
            for (int j = i + 1; j < cards.size() && !found; j++) {
                if (cards.get(i).getRank() == cards.get(j).getRank()){
                    found1 = cards.get(i).toString();
                    found2 = cards.get(j).toString();
            }
        }
        return found;*/
    }





}
